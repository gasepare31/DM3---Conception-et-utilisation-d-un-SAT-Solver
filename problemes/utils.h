#include <stdbool.h>
#include <stdio.h>  

/* renvoie la disjonction des formules de l */
char* au_moins_une(char** l, int n);

/* renvoie la formyle contenant au plus une variable vraie */
char* au_plus_une(char** l, int n);